import 'package:flutter/material.dart';

class BookmarkVeiw extends StatelessWidget {
  const BookmarkVeiw({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('bookmark'));
  }
}
